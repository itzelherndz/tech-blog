<main class="container-fluid">
    <div>
        <article class="container-fluid">
            <h2>{{post.title}}</h2>
            <h4>{{post.user.username}}</h4>
            <h5>{{post.createdAt}}</h5>
            <p>{{post.content}}</p>
            {{#if {eq post.user.username user} }}
            <button type="button" id="edit-button-{{post.id}}">Edit</button>
            <button type="button" id="delete-button-{{post.id}}">Delete</button>
            {{/if}}
        </article>
        <div>
            <h3>Comments:</h3>
            {{#each comments as |comment|}}
            <article class="container-fluid">
                <h3>{{comment.user.username}}</h3>
                <h5>{{comment.createdAt}}</h5>
                <p>{{comment.content}}</p>
            </article>
            {{/each}}
        </div>
    </div>
</main>